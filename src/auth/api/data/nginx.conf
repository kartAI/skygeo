events {
}

http {
    server {
        listen 80;
        server_name localhost;
        root /usr/share/nginx/html;
        location / {
        if ($arg_token != "demo123") { return 403; }
            try_files $uri $uri/ =404;
        }
    }
}
