{
  "name": "web",
  "version": "1.0.0",
  "scripts": {
    "prestart": "npm i",
    "start": "vite --open",
    "prebuild": "npm i",
    "build": "vite build",
    "test": "npm run build -- --base skygeo",
    "karttest": "scp -r -p dist/* karttest.nibio.no:/var/www/html/skygeo",
    "serve": "vite preview"
  },
  "devDependencies": {
    "vite": "latest"
  },
  "dependencies": {
    "ol": "latest"
  }
}
